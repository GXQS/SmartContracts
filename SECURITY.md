# Security

- Deterministic integer-only arithmetic (no floating point)
- Explicit runtime limits (memory, steps, call depth)
- Snapshot-based atomic state rollback
- Bounds-checked stack and memory operations
- Input length validation for PQC payload sizes
- Zeroization helpers for sensitive buffers
