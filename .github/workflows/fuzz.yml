name: fuzz
on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 1'
jobs:
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - run: go test ./tests -run Fuzz -fuzz=Fuzz -fuzztime=15s
